{
    "routes": [
      { "src": "/assets/(.*)", "dest": "/assets/$1" },
      { "src": "/(.*)", "dest": "/index.html" },
      { "src": "/content/(.*)", "dest": "/content/$1"}
    ],
    "outputDirectory": "dist"
  }
  